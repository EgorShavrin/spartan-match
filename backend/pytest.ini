[pytest]
# Put the backend folder on the import path so tests can do
#   from app.services.matcher import ...
# while still being run as plain `pytest` from the backend directory.
pythonpath = .
testpaths = tests
